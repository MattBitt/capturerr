<script setup lang="ts">
const route = useRoute()
const router = useRouter()
const id = ref(route.query.capture ? route.query.capture : '')

console.log('capture id', id)
</script>

<template>
  <div>
    <TairoContentWrapper>
      <template #default>
        <div><h1>This is the right page!</h1></div>
        <div>
          <h1>Does this work?</h1>
        </div>
        <div><h1>This is the right page!</h1></div>
      </template>
    </TairoContentWrapper>
  </div>
</template>
